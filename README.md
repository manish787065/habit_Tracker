# Habit Tracker App

A minimal, calming habit tracker built with Flutter, Riverpod, and Hive.

## 🚀 How to Run

Since the project files were generated manually, you need to install the dependencies first.

### 1. Run the Script
Simply double-click the **`run_app.bat`** file in this folder. 

It will automatically:
1.  Install dependencies (`flutter pub get`)
2.  Run the app (`flutter run`) using your specific Flutter path.

**Note**: Make sure an Android Emulator is running or a device is connected via USB before running the script.

## 📱 Features
*   **Colors**: Coral (#F16F6D) & Deep Purple (#5F4B8B).
*   **Dashboard**: Weekly day counter, Star ratings, Study timer.
*   **Stats**: Weekly Consistency Graph & Monthly Pie Chart.
*   **Habits**: Create and track daily habits.

## 📁 Project Structure
*   `lib/main.dart`: Entry point.
*   `lib/features/home`: Dashboard screens and widgets.
*   `lib/features/stats`: Charts and graphs.
*   `lib/features/habits`: Habit creation and tracking.
*   `lib/core/theme`: App colors and theme styling.
